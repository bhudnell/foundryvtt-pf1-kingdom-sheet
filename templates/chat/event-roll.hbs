<div class="event-card">
  <span class="flavor-text">
    {{ label }}
  </span>

  <div class="dice-roll">
    <div class="dice-result">
      <h3 class="roll-total dice-total has-details" data-natural="{{natural}}" data-bonus="{{bonus}}" data-total="{{total}}">
        <i class="fa-solid fa-dice-d20" inert></i>
        <span class="natural">{{d20}}</span>
        <span class="bonus">{{numberFormat bonus sign=true}}</span>
        <span class="equals">⇒</span>
        <span class="total">{{total}}</span>
      </h3>

      {{{details}}}
    </div>
  </div>

  {{#if eventOccurred}}
  <span>{{localize "PF1KS.EventOccurred"}}</span>
  <button class="roll-event">{{localize "PF1KS.RollEvent"}}</button>
  {{else}}
  <span>{{localize "PF1KS.NoEvent"}}</span>
  {{/if}}
</div>
